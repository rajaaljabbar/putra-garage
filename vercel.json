{
  "installCommand": "npm install",
  "buildCommand": "cd apps/ignition-app && npm run build",
  "outputDirectory": "apps/ignition-app/dist",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api" }
  ]
}
